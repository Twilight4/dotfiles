# Upgrading TTY (netcat shell)
python -c 'import pty; pty.spawn("/bin/bash")
*Ctrl-Z*
stty raw -echo
fg
*double-enter*

# check variables on properly-working prompt:
echo $TERM
stty size

# apply those inputs to netcat shell:
export TERM=xterm-256color
stty rows 67 columns 318
