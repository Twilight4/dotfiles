Extract the version of web servers, supporting frameworks, and applications:
  whatweb <TARGET_IP>

Web application enumeration across a network:
  whatweb --no-errors <TARGET_IP>/24

Classic:
  whatweb -a3 <TARGET_DOMAIN> -v

Options:
  -a3  Aggression level of 3
  -v   Verbose output
