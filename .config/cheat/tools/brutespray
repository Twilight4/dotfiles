# Options
  -f FILE, --file FILE  GNMAP, JSON or XML file to parse
  -o OUTPUT, --output OUTPUT
  -t THREADS, --threads THREADS - number of medusa threads
  -c, --continuous      keep brute-forcing after success
  -i, --interactive     interactive mode
  -v VERBOSE, --verbose VERBOSE verbose output from medusa [0-6], default=5
