secretsdump.py allows to retrieve all of the password hashes that this user account 
(that is synced with the domain controller) has to offer

Usage:
  secretsdump.py -just-dc <ACCOUNT_NAME>@<TARGET_IP>

Options:
  -just-dc              Extract only NTDS.DIT data (NTLM hashes and Kerberos keys)
