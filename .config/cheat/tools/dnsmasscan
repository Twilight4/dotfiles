Options:
  input_file        path to file containing domain names to resolve and scan
  dns_output_file   path to the file to store the DNS lookups in
  masscan_options   standard masscan options to use (excluding address range).
                    If not specified, will run masscan using the defaults of: 
                    -p1-65535 -oG masscan.log --rate=500
