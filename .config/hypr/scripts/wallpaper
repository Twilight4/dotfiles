#!/bin/bash

DIR=$HOME/.config/hypr/wallpapers
DIR=$HOME/.config/hypr/wallpapers/hack
PICS=($(ls ${DIR}))

RANDOMPICS=${PICS[ $RANDOM % ${#PICS[@]} ]}

if [[ $(pidof swaybg) ]]; then
  pkill swaybg
fi

swaybg -m fill -i ${DIR}/${RANDOMPICS}
notify-send "${RANDOMPICS}"
