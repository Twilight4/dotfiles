# Set the layout in this tab
layout tall
# Set the working directory for windows in the current tab
cd ~/workspace/dotfiles/.config/cheat/tools-charts/
# Create a window (title sets the tab title)
launch --title nnn sh -c "nnn -Ra"

# Create a new tab
new_tab navi
# Set the layout in this tab
layout tall
# Create a window
cd ~/workspace/cheats/active-directory/
# Create a window (title sets the tab title)
launch --title nnn sh -c "nnn -Ra"

# Create a new tab
new_tab cheats
# Set the layout in this tab
layout tall
# Set the working directory for windows in the current tab
cd ~/workspace/dotfiles/.config/cheat/tools/
# Create a window (title sets the tab title)
launch --title nnn sh -c "nnn -Ra"

# Create a new tab
new_tab templates
# Set the layout in this tab
layout tall
# Set the working directory for windows in the current tab
cd ~/workspace/dotfiles/.config/cheat/templates/
# Create a window (title sets the tab title)
launch --title nnn sh -c "nnn -Ra"

