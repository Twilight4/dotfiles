# Set the layout in this tab
layout tall
# Set the working directory for windows in the current tab
cd ~/
# Create a window (title sets the tab title)
launch --title agenda sh -c 'echo "\033[0;33mOpen agenda view with: \"M-x tl/org-agenda-o\"\033[0m"; emacsclient -nw --eval "(progn (tl/org-agenda-o) (delete-other-windows))"; exec zsh'